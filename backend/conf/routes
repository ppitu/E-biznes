
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

#User routers
GET /users controllers.UserController.getUsers
GET /users/:id controllers.UserController.getUser(id: Long)
POST /users controllers.UserController.addUser
PUT /users/:id controllers.UserController.updateUser(id: Long)
DELETE /users/:id controllers.UserController.deleteUser(id: Long)

#Product routers
GET /produts controllers.UserController.getUsers
GET /products/:id controllers.UserController.getUser(id: Long)
POST /products controllers.UserController.addUser
PUT /products/:id controllers.UserController.updateUser(id: Long)
DELETE /products/:id controllers.UserController.deleteUser(id: Long)
      